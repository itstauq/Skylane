name: CI

on:
  pull_request:
  push:

jobs:
  validate:
    runs-on: macos-15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Setup just cli
        uses: extractions/setup-just@v2

      - name: Run CI checks
        run: just test
