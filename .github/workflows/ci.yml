name: CI

on:
  pull_request:
  push:

jobs:
  validate:
    runs-on: macos-15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run CI checks
        run: ./scripts/ci.sh
